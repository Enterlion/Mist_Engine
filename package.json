{
  "name": "mist-engine-custom",
  "version": "1.0.0",
  "description": "Mist Engine for Foundry VTT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node -e \"require('fs').rmSync('dist', {recursive: true, force: true})\"",
    "compile": "tsc",
    "copy": "copyfiles -u 1 src/**/*.css src/**/*.json src/**/*.png src/**/*.webp src/**/*.html src/**/*.hbs src/**/*.mp3 src/**/*.wav src/**/*.otf src/**/*.woff2 src/**/*.ttf src/**/*.gif dist/ && copyfiles README.md dist/",
    "build": "npm run clean && npm run compile && npm run copy"
  },
  "keywords": [
    "RPG",
    "Foundry"
  ],
  "author": "Enterlion",
  "license": "",
  "devDependencies": {
    "@types/jquery": "^3.5.29",
    "copyfiles": "^2.4.1",
    "typescript": "^5.3.3"
  }
}
